# MIT License - Copyright (c) 2026 Destroyer
# AUR PKGBUILD for xdsk — Modern DSK image manipulation tool for Amstrad CPC
#
# To publish this package on AUR:
#   1. Create an AUR account at https://aur.archlinux.org
#   2. Clone the AUR repository: git clone ssh://aur@aur.archlinux.org/disc-image-studio.git
#   3. Copy this PKGBUILD into the cloned directory
#   4. Run: makepkg --printsrcinfo > .SRCINFO
#   5. Commit and push: git add PKGBUILD .SRCINFO && git commit -m "Initial release" && git push
#
# Replace GITHUB_OWNER with the actual GitHub username/organization.
# Update sha256sums on every release with: updpkgsums

# Maintainer: Destroyer <MAINTAINER_EMAIL>
pkgname=xdsk
pkgver=1.0.0
pkgrel=1
pkgdesc="xdsk - Modern DSK image manipulation tool for Amstrad CPC"
arch=("x86_64" "aarch64")
url="https://github.com/GITHUB_OWNER/xdsk"
license=("MIT")
makedepends=("rust" "cargo")
provides=("xdsk")
conflicts=("disc" "xdsk")
source=("$pkgname-$pkgver.tar.gz::https://github.com/GITHUB_OWNER/xdsk/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=("REPLACE_WITH_SHA256_OF_SOURCE_TARBALL")

prepare() {
    cd "$srcdir/$pkgname-$pkgver/xdsk"
    # Pre-fetch Cargo dependencies (respects CARGO_HOME cache)
    export RUSTUP_TOOLCHAIN=stable
    cargo fetch --locked --target "$CARCH-unknown-linux-gnu"
}

build() {
    cd "$srcdir/$pkgname-$pkgver/xdsk"
    export RUSTUP_TOOLCHAIN=stable
    export CARGO_TARGET_DIR="target"
    cargo build --release --frozen
}

check() {
    cd "$srcdir/$pkgname-$pkgver/xdsk"
    export RUSTUP_TOOLCHAIN=stable
    cargo test --release --frozen
}

package() {
    cd "$srcdir/$pkgname-$pkgver"

    # Binary
    install -Dm755 "xdsk/target/release/xdsk" "$pkgdir/usr/bin/xdsk"

    # License
    install -Dm644 "xdsk/LICENSE" \
        "$pkgdir/usr/share/licenses/$pkgname/LICENSE" 2>/dev/null || \
    install -Dm644 "LICENSE" \
        "$pkgdir/usr/share/licenses/$pkgname/LICENSE" 2>/dev/null || true

    # Man pages (generated by the binary itself)
    local mandir="$srcdir/man"
    mkdir -p "$mandir"
    "$pkgdir/usr/bin/xdsk" --no-color mangen "$mandir"
    for f in "$mandir"/*.1; do
        install -Dm644 "$f" "$pkgdir/usr/share/man/man1/$(basename "$f")"
    done

    # Shell completions
    local compdir="$srcdir/completions"
    mkdir -p "$compdir"

    "$pkgdir/usr/bin/xdsk" --no-color completions bash > "$compdir/xdsk.bash"
    install -Dm644 "$compdir/xdsk.bash" \
        "$pkgdir/usr/share/bash-completion/completions/xdsk"

    "$pkgdir/usr/bin/xdsk" --no-color completions zsh > "$compdir/_xdsk"
    install -Dm644 "$compdir/_xdsk" \
        "$pkgdir/usr/share/zsh/site-functions/_xdsk"

    "$pkgdir/usr/bin/xdsk" --no-color completions fish > "$compdir/xdsk.fish"
    install -Dm644 "$compdir/xdsk.fish" \
        "$pkgdir/usr/share/fish/vendor_completions.d/xdsk.fish"
}
